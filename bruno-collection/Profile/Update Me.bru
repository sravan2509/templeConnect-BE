meta {
  name: Update Me
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/users/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "name": "Updated Name"
  }
}
